import React from "react";
import { StyledHeader } from "./styles/Header.styled";

const Header = () => {
  return (
    <StyledHeader>
      <span>Button</span>
    </StyledHeader>
  );
};

export default Header;
